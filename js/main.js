/* =========================================================
   Eternal Bharat — Main Init
========================================================= */

document.addEventListener("DOMContentLoaded", () => {
    EB.setActiveNav();
    EB.initMobileNav();
});
